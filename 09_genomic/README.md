# 09 - Genomic structure

Exon-intron structure of each transcript from fastCDS (GENCODE v49), mapping
protein coordinates to CDS, UTR and intron blocks. `wire_isoform_genomics.py` and
`wire_genomic.py` run fastCDS and fold the result into the per-TF records.

# 07 - Activation-domain predictors

Per-residue activation propensity from two sequence models, over every canonical
protein and isoform: ADpred (`extract_adpred.py`) and PADDLE-noSS
(`extract_paddle.py`). Shown alongside the curated domains, not merged with them.
